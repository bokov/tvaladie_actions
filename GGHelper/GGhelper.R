# GGhelper
#
# Using data(x) and filling in iris, cars, etc ensures that a particular dataset is available
# How to get available datasets: data() - the human-readable version 
# data()$results - the data.frame version so you can use code on it
#
# Start out with a working webapp even if it doesn't do anything and then try and add functionality to it
# rather than trying to make functions work and then turn it into an app
#
# Get the functionality to work in the console and then when it works paste it in